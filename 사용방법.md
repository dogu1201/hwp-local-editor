# 내 문서 · HWP 로컬 편집기

한컴 한글 설치 없이 HWP 문서를 화면에서 직접 수정하는 Windows 프로그램입니다. RHWP 오픈소스 엔진과 편집 화면을 로컬에 포함했습니다.

## 시작하기

1. ZIP을 받았다면 폴더 전체를 압축 해제합니다.
2. **HWP-Local.exe**를 더블 클릭합니다. 함께 들어 있는 `rhwp` 및 `sdk` 폴더를 그대로 두세요.
3. 기본 브라우저에서 편집기가 열리면 **파일 열기**로 HWP 또는 HWPX 파일을 선택합니다.
4. 문서 안을 클릭해 글을 입력하거나 수정합니다. 표와 글자 모양은 편집기 메뉴에서 조절합니다. `Ctrl+Z`로 되돌릴 수 있습니다.
5. 위쪽 초록색 **수정본 HWP 저장**을 누릅니다. 브라우저가 `원래파일명_수정.hwp`를 다운로드합니다. 브라우저 설정에 따라 다운로드 폴더에 저장하거나 저장 위치를 묻습니다.
6. 결과 파일이 다운로드되었는지 확인한 뒤 편집기 탭을 닫습니다. 백그라운드 실행기도 약 20초 안에 자동으로 종료됩니다.

**기존 HwpEditor.exe와 다른 프로그램입니다. 새로 만든 HWP-Local.exe를 실행하세요.**

## 실행 환경과 저장

- Windows / .NET Framework 4 이상 / 최신 Edge 또는 Chrome 권장. Python, Node.js, 한컴 한글 설치 불필요. GitHub 배포본은 편집 엔진을 불러오기 위한 인터넷 연결이 필요합니다.
- 로컬 실행기는 `127.0.0.1`에만 연결됩니다. 필요한 편집기·글꼴·WASM 파일을 포함하며, 외부 리소스 요청은 콘텐츠 보안 정책으로 차단합니다.
- 사용자가 선택한 문서는 브라우저에서 처리합니다. 서버에 문서 업로드 기능은 없습니다.
- 위쪽 저장 버튼은 수정본을 다운로드하며 원본 파일에 직접 쓰지 않습니다. 편집기 내부 메뉴의 저장 기능은 RHWP 기본 동작을 따릅니다.
- 다운로드 완료는 브라우저가 관리하므로 도구의 표시가 “다운로드 요청 완료”여도 실제 파일을 확인하세요. 저장 취소나 다운로드 차단이 있을 수 있습니다.
- RHWP의 로컬 자동복구 안내가 다음 실행에 나타날 수 있습니다. 다운로드 성공 여부를 프로그램이 확인할 수 없어 복구 데이터를 임의로 지우지 않습니다.
- 별도의 실행기 창은 표시되지 않습니다. 편집기 브라우저 탭을 닫으면 로컬 서버도 자동으로 종료됩니다.

## 제공한 두 문서로 확인한 내용

| 문서 | 쪽 수 | 확인 결과 |
|---|---:|---|
| 학생산출물_토의토론(개별+제출)_03_1.hwp | 3 | 열기, 첫 쪽 렌더링, 한글 직접 입력, 수정 후 HWP 내보내기·재열기 |
| 학생산출물_토의토론(개별+제출)_04_1 (1).hwp | 2 | 열기, 첫 쪽 SVG 생성, 텍스트 수정 후 HWP 내보내기·재열기 |

두 문서 모두 수정 직후와 재열기 후의 추출 텍스트가 일치했고 쪽 수가 유지됐습니다. 원본 파일은 변경하지 않았습니다. 테스트는 RHWP 엔진 기준이며, 한컴 한글에서 결과 파일을 재검증하지는 않았습니다. 전용 글꼴, 복잡한 표·수식·개체는 원본 한글과 표현이 다를 수 있으므로 제출 전에 화면과 수정본을 확인하세요.

## 구성과 라이선스

- `Launcher.cs`: 로컬 서버 및 Windows 실행기 소스
- `index.html`, `app.js`: 파일 열기·수정본 다운로드 화면
- `sdk/`: @rhwp/editor 0.8.6
- `rhwp/`: RHWP 공식 공개 배포본을 로컬로 포함. 서비스 워커 자동 등록 태그만 제거했습니다.
- `LICENSE`, `THIRD_PARTY_LICENSES.md`: RHWP 및 포함 구성 요소 라이선스
- `검증결과.json`: 두 문서의 저장·재열기 검사 기록. 문서 본문은 포함하지 않습니다.

RHWP 원저작자: Edward Kim. [원본 소스](https://github.com/edwardkim/rhwp) · [편집기 패키지](https://www.npmjs.com/package/@rhwp/editor)

이 프로그램의 Windows 실행기와 외부 화면은 사용자 요청에 맞춰 작성했습니다. HWP 편집 엔진은 RHWP이며, 한글과컴퓨터와 제휴 관계가 없습니다.

